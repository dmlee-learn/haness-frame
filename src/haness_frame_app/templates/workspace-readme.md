# Runtime Workspace

Project:

```text
$project_name
```

Working description:

```text
$working_description
```

This folder is used by the runnable harness engine for state, role packets,
and execution artifacts.

## Runtime Files

```text
state.json        Project metadata and live harness state
services.json     Resolved role-to-service snapshot with URLs and models
packs/            Rendered role packets
evidence/         Saved research notes and citations
decisions/        Decision snapshots and approvals
```
